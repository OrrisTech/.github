# Reusable workflow: SEO & Lighthouse
# Runs Lighthouse CI (lhci) to check performance, accessibility, and SEO scores.
# Auto-detects package manager (pnpm > npm > bun) based on lockfile.
# Runs if a lighthouserc config exists or if urls are explicitly provided.
name: CI - SEO & Lighthouse

on:
  workflow_call:
    inputs:
      node_version:
        description: "Node.js version to use"
        required: false
        type: string
        default: "22"
      working_directory:
        description: "Working directory for monorepo support"
        required: false
        type: string
        default: "."
      lighthouse_urls:
        description: "Comma-separated URLs to audit (default: http://localhost:3000)"
        required: false
        type: string
        default: "http://localhost:3000"
      threshold_performance:
        description: "Minimum performance score (0.0 - 1.0)"
        required: false
        type: number
        default: 0.7
      threshold_accessibility:
        description: "Minimum accessibility score (0.0 - 1.0)"
        required: false
        type: number
        default: 0.9
      threshold_seo:
        description: "Minimum SEO score (0.0 - 1.0)"
        required: false
        type: number
        default: 0.9

jobs:
  lighthouse:
    name: Lighthouse Audit
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ${{ inputs.working_directory }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check if Lighthouse should run
        id: check-lh
        env:
          LH_URLS: ${{ inputs.lighthouse_urls }}
        run: |
          HAS_CONFIG="false"
          if [ -f "lighthouserc.json" ] || [ -f ".lighthouserc.json" ] || [ -f "lighthouserc.js" ] || [ -f "lighthouserc.yml" ]; then
            HAS_CONFIG="true"
          fi

          CUSTOM_URLS="false"
          if [ "$LH_URLS" != "http://localhost:3000" ]; then
            CUSTOM_URLS="true"
          fi

          if [ "$HAS_CONFIG" = "true" ] || [ "$CUSTOM_URLS" = "true" ]; then
            echo "should_run=true" >> "$GITHUB_OUTPUT"
            echo "has_config=$HAS_CONFIG" >> "$GITHUB_OUTPUT"
          else
            echo "should_run=false" >> "$GITHUB_OUTPUT"
            echo "::notice::No Lighthouse config found and no custom URLs provided — skipping audit"
          fi

      - name: Detect package manager
        if: steps.check-lh.outputs.should_run == 'true'
        id: detect-pm
        run: |
          if [ -f "pnpm-lock.yaml" ]; then
            echo "manager=pnpm" >> "$GITHUB_OUTPUT"
            # Detect compatible pnpm version from packageManager field or lockfile
            PNPM_VER=""
            if [ -f "package.json" ]; then
              PNPM_VER=$(node -e "try{const v=require('./package.json').packageManager;if(v&&v.startsWith('pnpm@'))console.log(v.split('@')[1])}catch(e){}" 2>/dev/null)
            fi
            if [ -z "$PNPM_VER" ]; then
              LF_MAJOR=$(grep -m1 'lockfileVersion' pnpm-lock.yaml | grep -oE '[0-9]+' | head -1)
              case "$LF_MAJOR" in
                9) PNPM_VER="9" ;;
                6) PNPM_VER="8" ;;
                5) PNPM_VER="7" ;;
                *) PNPM_VER="latest" ;;
              esac
            fi
            echo "pnpm_version=$PNPM_VER" >> "$GITHUB_OUTPUT"
          elif [ -f "package-lock.json" ]; then
            echo "manager=npm" >> "$GITHUB_OUTPUT"
          else
            echo "manager=bun" >> "$GITHUB_OUTPUT"
          fi

      - name: Setup pnpm
        if: steps.check-lh.outputs.should_run == 'true' && steps.detect-pm.outputs.manager == 'pnpm'
        uses: pnpm/action-setup@v4
        with:
          version: ${{ steps.detect-pm.outputs.pnpm_version }}

      - name: Setup Bun
        if: steps.check-lh.outputs.should_run == 'true' && steps.detect-pm.outputs.manager == 'bun'
        uses: oven-sh/setup-bun@v2

      - name: Setup Node.js
        if: steps.check-lh.outputs.should_run == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: ${{ inputs.node_version }}

      - name: Install dependencies
        if: steps.check-lh.outputs.should_run == 'true'
        run: |
          case "${{ steps.detect-pm.outputs.manager }}" in
            pnpm) pnpm install --frozen-lockfile ;;
            npm)  npm ci ;;
            bun)  bun install --frozen-lockfile ;;
          esac

      - name: Install Lighthouse CI
        if: steps.check-lh.outputs.should_run == 'true'
        run: npm install -g @lhci/cli

      - name: Run Lighthouse CI (with config)
        if: steps.check-lh.outputs.should_run == 'true' && steps.check-lh.outputs.has_config == 'true'
        run: lhci autorun

      - name: Run Lighthouse CI (with inputs)
        if: steps.check-lh.outputs.should_run == 'true' && steps.check-lh.outputs.has_config == 'false'
        env:
          LH_URLS: ${{ inputs.lighthouse_urls }}
          LH_PERF: ${{ inputs.threshold_performance }}
          LH_A11Y: ${{ inputs.threshold_accessibility }}
          LH_SEO: ${{ inputs.threshold_seo }}
        run: |
          URLS_JSON=$(echo "$LH_URLS" | node -e "
            const input = require('fs').readFileSync('/dev/stdin','utf8').trim();
            const urls = input.split(',').map(u => u.trim()).filter(Boolean);
            console.log(JSON.stringify(urls));
          ")

          cat > /tmp/lighthouserc.json << LHEOF
          {
            "ci": {
              "collect": {
                "url": ${URLS_JSON},
                "numberOfRuns": 1
              },
              "assert": {
                "assertions": {
                  "categories:performance": ["warn", { "minScore": ${LH_PERF} }],
                  "categories:accessibility": ["error", { "minScore": ${LH_A11Y} }],
                  "categories:seo": ["error", { "minScore": ${LH_SEO} }]
                }
              },
              "upload": {
                "target": "temporary-public-storage"
              }
            }
          }
          LHEOF

          lhci autorun --config=/tmp/lighthouserc.json

      - name: Upload Lighthouse results
        if: steps.check-lh.outputs.should_run == 'true' && always()
        uses: actions/upload-artifact@v4
        with:
          name: lighthouse-results
          path: ${{ inputs.working_directory }}/.lighthouseci/
          if-no-files-found: ignore
          retention-days: 7
